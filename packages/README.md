# `/packages/mcp` — `@assembl/mcp` npm package

Source for the official Assembl Model Context Protocol server. See [`mcp/README.md`](./mcp/README.md) for usage docs.

This directory is intentionally **outside** the React app's `src/` and is **not** built or shipped as part of the Lovable preview. It's only used when you publish the npm package.
