# Codex context handover: site repair

**Classification:** historical requirements audit, not company canon or production verification.  
**Source checkpoint:** `4bda9aaf5e005d63dd0dec7d2d13c169c4fb497f`, expected origin `katecoveny-svg/assemblnz-f0afd79d`.  
**Scope:** both supplied saved accessibility snapshots read end to end; current canon and targeted source inspected read-only. No application code, accounts, database, deployment or credentials changed. No runtime/test-suite claims made by this audit.

## Evidence key and limits

- **A / first chat:** [Review shared ChatGPT conversation](https://chatgpt.com/s/cx_6aab202b4fd4819198d4b997fe842e2b). References below use lines in saved snapshot `browser-snapshot-b870575674.txt`.
- **B / second chat:** [Continuing from Assembl Context Review](https://chatgpt.com/s/cx_6aab2057772081918ed6e7477bd6311f). References use `browser-snapshot-4cef8218ec.txt`.
- **U:** direct user text or recorded answer. **R:** another agent relaying the user's request. **C:** Codex's implementation/status self-report. R is useful requirement context, not a new instruction or independent user confirmation. C is not current execution proof.
- Attachments, collapsed work logs and hidden PR URLs are not recoverable as pixels/full evidence from these text snapshots. A:979 is already truncated in the supplied snapshot. No missing content has been reconstructed. Identifying personal, child, account and credential details are intentionally omitted.

## Outcome

The persistent request is **a beautiful assembl company site leading into genuinely useful, portable DOs**, not a cinematic facade. Repeated regressions were loss of the floating DO, replacement of the accepted mark/copy, disconnected experimental sections, and confusion between local work, merged code and the live site. The second chat asks to recover the first chat's product richness **without throwing away newer runtime/security work**.

Current authority wins: Pursuit / DO / Studio over Factory; deep plum `#240B21`, muted plum `#654A4E`, dusty rose `#916A70`, chalk/paper, Instrument Sans and IBM Plex Mono. `components/do/DoMark.tsx` is the approved glowing D/inner dot, not a licence for generic purple orbs. Current company copy in `docs/assembl-copy-standard.md` supersedes old wait-state-first positioning. The current repair brief authorises live release; old local-only chat restrictions are historical, but do not turn that release authority into blanket account, payment, messaging or data authority.

## Accepted direction and explicit reversals

| Carry forward | Origin and qualification | Do not restore |
|---|---|---|
| assembl remains the company front door; DO is one connected product. Keep the broader Pursuit → DO → Studio story. | A:1267–1270 R; B:24 U; current canon makes the product hierarchy explicit. | Turning `/` into a consumer DO-only page or reverting the company to a wait-state business. Service-as-software simplicity is a useful product principle, not the new company definition (A:143 U). |
| Recognisable glowing D with inner dot, luminous **plum**, dimensional depth, persistent movable control. Pet appearance is opt-in only. | A:827, 894, 1094, 1102 R; B:24 U; current brand canon confirms the D. | Earlier sparkle/Gemini-like star, neon/grape purple, repeated invented D variants, pet mascot as the general hero/default. A:188 U is an earlier orb request, superseded in detail. |
| Option two: visual customisation canvas, modules, context review, saved arrangements, useful compact/phone preview. | A:930 U selects option two; A:933 C describes it; A:832, 865, 1291 R refine it. | A text-only form reskinned as a product; a decorative phone frame with no usable controls. |
| Preserve the historically approved DO builder heading `assembl your DO`; keep company homepage copy governed separately. Product `DO`, attribution `DO by assembl`, specialist `Builder DO`. | A:957, 1142 R; B:232–236 C; current brand docs confirm naming. | `DOO`, `Builderdoo` in public copy, unsolicited slogans, thick/wrong wordmark type, cutesy “little helper” language. A:855 and 1142 R explicitly reject examples. A personal user-entered name is not a product rename. |
| One coherent visual-first scroll: D opens into selectable parts → actual canvas → shared context → editable useful result. Include demonstrations of movement and explicit capture. | A:1251–1299 R; B:30 U demands motion, video, parallax and real usefulness. | Cinema/arrival/orbit mode controls as the customer journey; generic abstract decoration; a new hero followed by the old landing page. A's earlier cinema-controls brief (1189–1223 U) was subsequently revised, not an instruction to restore all modes. |
| Premium 3D/motion is still wanted, with meaningful product causality and mobile/static/reduced-motion fallbacks. | A:1291 R clarifies that rejecting abstraction does not reject impressive motion; B:30 U. | Treating a camera fly-through, a plain 3D office or generated picture as the finished product. Spark/Gaussian splats were an investigation, not a selected stack or a quality fix (B:246–276 C). |
| Remove the rejected bird/fish/gannet treatment from DO and the complained-about bird homepage treatment. | A:1155 R; B:24 U. | Reusing A's early fish/gannet request (68 U) as the latest design decision. Current canon still permits relevant narrative natural-assembly work; the rejected implementation is not a universal ban on all nature imagery. |
| Keep specialist character options, but public Pet DO/Dachshund naming and scope. | A:860, 1094 R; A:1165 U confirms pet-only use while main identity stays D. | Publishing the private inspiration's name, inventing another generic pet asset, or making a legacy internal storage identifier a customer-facing label. |
| Preserve useful backend work and make connected states honest. | B kickoff:13; B:30 U complains about a facade; B:114 U asks what is done, unfinished and should change. | Replacing the runtime to chase a design, counting a toggle/link as a connection, or claiming “done” for a draft. |

## Prioritised handover and acceptance checklist

### P0 — repair the real journey, not only its opening shot

- [ ] **Pin exact served routes and release identity.** A:1267–1270 R distinguishes live `/` from local `/do`; B:37 U asks for work to reach main automatically. Current `app/page.tsx:1–27` serves `AssemblTheWorkHome`; `app/do/page.tsx:1–12` serves `DoHome`, not the still-present `DoHomeCurrent`. Verify the actual production commit, assets, all-scroll composition and route transitions. Preserve approved copy/mark and useful functions while improving the whole experience. Do not blindly merge old PRs or restore an unused component.
- [ ] **Prove one real useful task from the public front door.** Choose DO → share approved harmless context → execute → editable result → save/reopen or explicit export → receipt/status. A:146 U rejects generic, unhelpful tasks; B:30 U rejects unconnected “dudes”. Start with writing and weekly/school admin, plus the current Meeting DO entry point. Exercise actual task runtime; an illustrative weekly-plan animation is not completion evidence. Show provider/auth/usage failures rather than leaving decorative controls.
- [ ] **Unblock and verify sign-in.** B:70 U cannot sign in; B:74 U says the email never arrived. “Request accepted” was not delivery proof (B:80–87 C). Test request → actual delivery → user-opened link → session → authorised DO controls, without reading private login links. Audit redirect/error/resend states. Do not assume the historically mentioned mail provider is still the live cause.
- [ ] **Preserve and prove privacy boundaries before claiming connected memory.** B:68,72 U separately approved one database privacy fix and four service fixes; B:87 C still had service release blocked. Inspect current migration/service deployment state and test anonymous denial, cross-account denial and valid-owner access. A green build or local migration file is not applied RLS. Do not reopen unrelated live database changes under homepage authority.
- [ ] **Restore and test the visible floating control across the whole journey.** A:894,1094,1142 R and B:24 U repeatedly identify its disappearance/drift. Verify default D, drag versus click, keyboard movement, clamping after resize, mobile non-obstruction and an actual workspace opening. Dragging alone must not capture or grant access. Finish full-scroll desktop/375px/reduced-motion/no-WebGL checks, not just a hero screenshot.

### P1 — make portability and persistent work real, with honest boundaries

| Requirement | Origin | Current source evidence; concrete unfinished verification/work |
|---|---|---|
| **Natural NZ voice plus typed conversation and task continuity** | A:317,333,378–386 R; A:784,807 R report failed audio; A:979 R gives memory/conversation highest priority. | `app/do/live/page.tsx:8` redirects outside development; `/api/do/live:53–65` deliberately rejects non-local production use. `live-session-server.ts:34–38` keeps local sessions in a process map. Historical OpenAI session creation success is **not hosted conversational DO**. Preserve the separate hosted Gemini path; prove audible two-way conversation, device selection, interruptions versus task cancellation, corrections, parallel task progress, restart/cleanup and human-reviewed NZ accent. Revalidate historical GPT-Live/Agents API availability against current official docs before choosing architecture. Do not remove the local-only guard to make it “live”. |
| **Meeting recording → transcript → smart notes → tasks/owners/delegation** | A:146 U; B:160 U and 165 U (“all of above”, audio options not visible). | `MeetingDo.tsx:270` still stops at **10 minutes**; `:571` warns users to download before closing. Recording is browser-local. Transcription requires sign-in and configured Deepgram, with a **4 MB** upload limit (`app/api/do/meetings/transcribe/route.ts:5–32`). Full-length capture, resumability, browser/phone/background behaviour, actual meeting audio and durable approved delegation need proof/work. Record/stop/download UI is not full meeting-product readiness. |
| **Gmail inbox/school admin and a Gmail add-on** | A:152 U selects Gmail first; B:160 U asks for an add-on and meeting workflow. | Current connection route enforces owner sign-in and provider/app setup (`app/api/do/connections/route.ts:72–82`); `DoHome.tsx:74` calls it a school-admin pilot. Prove account connection → user-selected message → dates/forms/payments/gearlist result. Separately verify Gmail add-on installation and its handoff. Browser extension beside Gmail, an embed ZIP and a Gmail add-on are three different deliverables (B:217–228 C). |
| **Mac floating companion, menu-bar access, reviewed messaging help and restart persistence** | A:193 U chooses browser and desktop; A:317,321 R requests reviewed Messages/WhatsApp drafts, not sending; A:1320 R requests drag-to-Applications install; B:24 U asks for shutdown persistence. | Swift source contains selected-text capture/reviewed paste, drag handling, saved coordinates, menu bar and opt-in `SMAppService` login registration (`DOCompanion.swift:83–142,340–435`). These are not absent and should be extended, not rebuilt. B:349–369 C leaves dragging/restart unverified and notarisation open. Prove clean install, launch, drag-versus-open, relaunch/login restoration, compatible text handoff and permissions; verify actual signature/notarisation/distribution before public-ready claims. General autonomous desktop control is not established. |
| **Browser/phone vision and explicit context sharing** | A:827,979,1299 R; B:40 U explicitly asks DO to see shown work. | `DoVision.tsx:81–150` implements image upload and a user-chosen display still; capture is separate from consent/use. Recover/use the existing path, do not claim vision missing merely because it was absent early in A. Prove screenshot/photo → local preview → explicit provider consent → observation → edit → add to task. The inspected native helper is selected-text/reviewed-paste, not a verified native screen-capture bridge. Also verify selected-text/current-page and PDF/file sharing separately; image upload alone does not fulfil every context channel. Prove browser/PWA fallbacks; no ambient screen surveillance. |
| **Memory, profiles and saved editable outputs** | A:674,740,979 R; A:1024 C reports only device-local memory. | `app/do/live/DoMemory.tsx:27,35` uses `localStorage`. This is not secure separation between people sharing a browser or authenticated cross-device personal/child/client memory. Verify view/edit/delete/disable, explicit inclusion in prompts, scope isolation, storage disclosures and deletion effects. Travel saving was claimed locally, not cloud sync. Separate profile/trip state from Office job persistence. |
| **Persistent jobs, receipts, needs-you state and Office handoffs** | B:13 kickoff; B:156 C lists priorities. | Newer source **does** contain authenticated Builder/Office storage: `/api/do/builder/jobs` and `apps/do/services/office-jobs.ts`, plus current persistence/event migrations. Do not repeat “all Office storage is missing”. However the service falls back to process memory on missing-schema/env errors (`office-jobs.ts:143–146`), while the list API maps records to `durable: true` (`jobs/route.ts:62–78`). Verify DB-backed storage and truthful provenance, restart/cross-device persistence, idempotency, owner isolation, real handoffs and completion evidence. A saved/accepted job is not executed work. |
| **Builder DO actually builds, not only plans** | B:232–236 C explicitly leaves automatic execution unconnected. | The inspected UI calls plan/save/reopen APIs; planning returns authority boundaries (`builder/plan/route.ts:92–99`). Source proves contracts and storage, not an attached worker executing code. Find and exercise an authorised harness path or label execution unavailable. Preserve model-agnostic routing; validate a bounded branch/build/test artifact and receipt before claiming automation. |
| **Phone install/share and customer downloads** | A:745,1320 R; A:258–283 C describes unpublished PWA work. | Routes for install/share/service worker/downloads exist. Reconcile current artifacts instead of assuming PR #1234 is still pending. Test real iOS/Android install/reopen/share behaviour, explicit target profile and imported-content review, paste/upload fallback and offline honesty. PWA shell caching is not offline agent execution or durable cloud sync. |
| **White-label DO from Pursuit Hub** | A:1278 R asks specifically for a client-scoped generator, not a generic DO link. | `PursuitLanding.tsx:84–85` now links Task DO Maker; public playground also links `/studio/do-maker`. Current canon says preview/export is distinct from durable Office provisioning. Verify the authenticated **Hub** path, client branding/purpose/permissions, saved client-scoped DO and handoff. A public landing link does not close this request. |

### P2 — retain the remaining product backlog without claiming it is connected

- [ ] **Shared specialist capabilities, not cosmetic presets** — A:674,939,947,952,971 R: personal/work/business/child/client profiles on one platform; per-template instructions, tools, domain retrieval and permitted user context. Recover useful legacy NZ roles with provenance rather than restoring old marketplace architecture or old law as truth. Research, writing, grammar, study, newsletter parsing and creative/image tasks should be selectable and genuinely usable.
- [ ] **Live public web search plus separately verified NZ retrieval** — A:939,971,1066,1283 R. Search should be available to every template/custom recipe, with citations, source dates, primary-source preference and honest unavailable states. A:1055,1072 C reports a local cited result/settings test only. Verify saved search permissions reach the executing runtime. NZ RAG/government/Pursuit feeds are a separate integration: public web search is not proof of live internal feeds, licensed access or cross-tenant entitlement.
- [ ] **General scheduled checking/reminders** — A:979,992 R. Persist criteria, supported source, schedule, notification preference, pause/delete, deduplication and meaningful-change results; prove execution with app/browser closed. A:1074 C says draft validation/change detection existed but worker/notifications were unconnected. **Do not revive Trade Me-specific work:** A:992 explicitly withdrew that branch. No real user watch was requested.
- [ ] **Practical life-admin templates** — A:146,188 U; A:730–740,971,1309–1312 R. Bills/photo reading and source-backed cheaper alternatives; flights, appointments, travel/hotels and editable itinerary/maps/packing/PDF; meal planning from budget/preferences/pantry with reviewed supermarket cart; Legal DO navigation; Mariner tides/weather/fishing; age-appropriate study/teen organisation and opt-in parent-group notices. Verify current source/licensing/access for each. Booking, payment, notifications, transport accounts, group sharing and monitoring remain distinct approved actions, not implied by a template card.
- [ ] **Pricing and payments** — A:105 U chose **three free tasks, then enquire**; A:832,850,1150 R requests sustainable NZD allowances and test-mode checkout; A:1363 U says access was given, with no subsequent success result. Historical per-network trial enforcement is not durable customer metering. Inspect current billing ownership/configuration, prove caps, test checkout/webhook/idempotency/entitlements, disclose economics assumptions and obtain pricing/live-payment approval. No approved tier prices, successful checkout or live billing activation is evidenced by either snapshot.
- [ ] **Contact and legal launch details** — A:103 U wants the form to open the visitor's email draft to the published business address; A:127–133 C correctly says the visitor must press Send. Verify the current mailto flow rather than promise automatic delivery. A:899–927 R asks for visible Terms/Privacy, actual-data-flow and NZ official-source research, a claim-by-claim security/privacy gap matrix and legal review. Verify entity/contact, provider processing/storage/retention, child access, consent/notices, access/correction/deletion, overseas handling and breach arrangements. Do not inherit another provider's compliance guarantees or generalise one database-region claim to all data.
- [ ] **Writing DO used for copy, not falsely credited** — A:1325 R explicitly asks for an actual bounded Writing DO run and editable draft. A:1353–1359 C says that run was blocked. Recheck approved nonprivate context and execute only within current scope; never claim manually written copy came from DO. Preserve accepted headings and review its output before replacement.
- [ ] **Optional business-use/wait-state demonstration** — A:1115 R asks for a compact editable example next to relevant rewards content, not another homepage rebuild. Keep real wait, optional participation/skip, explicit illustrative status and no invented partner/reward or sponsor influence over answers. Current canon places this within the broader journey/DO capabilities.

## Historical claims that must not become release evidence

| Snapshot report | What can safely be carried forward |
|---|---|
| A:111–137 C: deployed builder, working writing/image generation, three-task network gate and 58 tests. | Recover/verify those paths; historical test counts are not current tests. Visual browser checks were explicitly blocked. |
| A:155–178 C: Gmail pilot deployed, 78 tests, inbox read-access inactive. | Gmail-first intent is clear. Connection, selected-message processing and actual current permissions remain runtime checks. |
| A:200–244 C: bills comparison, browser companion, Mac development build, later bill-photo reading; 90/104 tests. | Features/source may be reusable. Earlier photo-reading gap was later claimed closed; ongoing price watches and autonomous desktop control were not. Verify each separately. |
| A:611–646,1045–1060 C: OpenAI authentication/session test, correction test, speaker playback started and cited search. | No proof the user heard a working live conversation or accepted NZ accent. The current recovered OpenAI path is explicitly local-only. |
| A:1333–1359 C: local D-reveal/weekly-admin preview and Mac development installer; mobile/reduced-motion checks. | Useful recovery pointers, not a published release or public-ready installer. Writing DO task remained blocked. |
| B:68–87 U/C: privacy fix applied; separate four-service release authorised but preview failed. | Reconcile actual deployed schema/services. Preserve fixes; do not copy historical permission grants into new unrelated work. |
| B:124–156 C: PR #1293, typography/identity/sign-in/connection/improvement fixes; 167 tests. | Claimed unreleased at that moment; verify current equivalents. No need to reimplement things already merged. |
| B:168–191,232–236 C: draft PR #1294, meetings/add-on source and Builder naming/planning. | Full meeting capture, Gmail installation and automatic build execution remained open; current recorder still has the stated limit. |
| B:291–407 C: PR #1295 Pursuit refresh, Mac changes, walkthrough and companion links. | Repeatedly stated not merged/deployed; final Mac drag/restart and walkthrough quality remained unresolved. Current PR/deploy status was not checked in this audit. |

## Concrete source cautions for the repair owner

1. **Legacy recovery docs are not all current truth.** `docs/DO-VISUAL-BASELINE.md` is useful provenance for displaced UI, but still says “Builderdoo” and “Office demo/local” while current canonical naming and newer storage source differ. Prefer current canon/source; preserve the visual recovery evidence without restoring stale runtime statements.
2. **Do not overdiagnose an unused code path.** `apps/do/shared/spine/openai-adapter.ts:137–143` still discards its SDK router and says “Never become a messaging assistant”. This corroborates the old warning, but does not prove every newer runtime uses that path. Trace the active task route before changing it; reviewed messaging drafts must not become automatic sends.
3. **Full-page consistency still deserves inspection.** `DoHome` has Personal/Work specialist cards and `DoReveal`; `DoAppearance` re-exports the shared DoMark correctly. It also retains a copper colour choice (`DoAppearance.tsx:44,85`) and `DoHome` defaults a name to “My DO”. Assess rendered labels/scope against current canon instead of blanket string replacement or deleting compatibility data.
4. **Finish with a proof matrix.** For every promoted DO state, record exact route/build, available vs configured vs tested, harmless test input, editable output/receipt, browser/native/device scope, failure state and remaining prerequisite. Mark preparations and simulations honestly. A polished animated world and a successful build are not substitutes for that evidence.

### Authority boundary

Use existing authorised infrastructure and secure setup; do not copy historic account identifiers, credentials or personal context into code or reports. User-granted access is scoped app/file/service permission, not authority to send, purchase, record bystanders, read unrelated material, publish private client data or disable OS security. Phone, native app, extension, Gmail add-on and embedded website DO remain separate surfaces with separately tested permission and installation flows.

**Recommended order:** stabilise current company/DO identity and exact routes → prove auth/privacy and a useful end-to-end task → restore portable launcher/handoffs → finish meeting/voice/persistence evidence → connect remaining specialist capabilities → verified release and full-scroll product proof. Do not postpone functional repairs until after another standalone 3D experiment.
