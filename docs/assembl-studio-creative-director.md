# assembl Studio — Creative Director PREVIEW v0

**Draft / PREVIEW only.** Do not treat as live homepage craft.

## Where it lives

| Surface | Path | Role |
|---|---|---|
| Creative Director pipeline | `/creative-studio` | idea → direction → world → experience |
| Generative Studio craft | `/generative-studio` | shaders / materials / export (unchanged) |
| Ensemble sell door | `/agents/ensemble` | links into Studio craft |
| Agent builder | `/studio` | koro workbench (unchanged) |
| Live homepage | `/` | **untouched** (`CinematicJourneyHome`) |

## Pipeline

1. **Understand** — `compileCreativeIntent()` → CREATIVE INTENT block  
2. **Art direct** — `inventArtDirections()` → exactly 3 directions  
3. **Visual targets** — DEMO plates for desktop / mobile / interaction / storyboard  
4. **Construct** — editorial CSS/GSAP · cinematic video · spatial R3F  
5. **Critic** — `runCreativeCritic()` checklist; revise reasons on fail  

Seed: `seedEnergyDemo()` — fictional NZ energy demonstrator.

## Registry

See `packages/registry/README.md` for `@assembl/*` blocks, AI install notes, and licensing caution.

## Craft continuity (2026-09-14)

`/creative-studio` borrows Ensemble creative-desk DNA (`ens-root` patterns): sticky nav + DEMO badge, editorial hero with desk plate (not architecture floor-plan), CraftScroll (Lenis/GSAP), dense direction boards, chalk/paper field with plum accent only. Generative Studio remains the linked craft tool. Arc/Customs BlueprintScene / PlanPins are never used.

## Success check

Kate can open `/creative-studio`, run **energy DEMO**, see 3 genuinely different directions, pick one, see construct + critic output, and read the registry README. At a glance it should read as Ensemble / Gen Studio family — not Arc floor-plan craft.
