# swap <client-slug>, then: wrangler pages project create assembling-<client-slug> --production-branch=main
name = "assembling-<client-slug>"
compatibility_date = "2025-10-01"
pages_build_output_dir = "."

[ai]
binding = "AI"
