/**
 * Spatial C: single web launcher = GlowDoWidget.
 * Thin compatibility wrapper so older imports resolve to the same orb.
 */
'use client';

export { GlowDoWidget as DoFloatingWidget } from '@/components/site/assembl-the-work/GlowDoWidget';
