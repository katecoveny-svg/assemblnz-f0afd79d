import { redirect } from "next/navigation";

const PUBLIC_MAKER_URL = "https://make-with-assembl.katecoveny.chatgpt.site";

export default function CreativeStudioPage() {
  redirect(PUBLIC_MAKER_URL);
}
