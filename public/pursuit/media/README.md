# Pursuit hero media — Kate drop-in

## Required final asset

Replace the PREVIEW craft loop with a **muted** screen recording of the real Pursuit canvas:

1. Open the Assemble Pursuit hub (ChatGPT).
2. Record: agent-suggested parts rearrange on the canvas → move into a brand / client frame.
3. Export a short muted loop (about 6–12 seconds), soft grade, plum-safe.

## Paths (keep these names)

| File | Role |
|---|---|
| `pursuit-canvas-loop.mp4` | Primary muted loop (H.264) |
| `pursuit-canvas-loop.webm` | Optional VP9 twin |
| `pursuit-canvas-poster.jpg` | First-frame / reduced-motion poster |

Absolute public URLs:

- `/pursuit/media/pursuit-canvas-loop.mp4`
- `/pursuit/media/pursuit-canvas-loop.webm`
- `/pursuit/media/pursuit-canvas-poster.jpg`

## Current state

The committed files are a **clearly labelled PREVIEW** craft loop (NZ signals → canvas rearrange → brand demonstrator). They are not a live hub recording.
