# TOA walk-through — tone reference (mood only, DO NOT SHIP)

`brand-film-scene-3.mp4` — Kate's brand-film scene 3 (narrated), the cinematic
tone reference for the 16A walk-through. It is NOT an app asset and is never
embedded: the walk-through is real 3D, graded and paced *against* this film.

The `.mp4` here is git-ignored (over the ship threshold). Kept in the working
tree so future work can screen it. See `components/ops/toa/walk/` for how its
language (pacing, palette, POI cadence, phase transitions) is matched.
