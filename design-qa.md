# Homepage design verification

Selected direction: option 2, plum ocean assembly. User amendments: dense fish schools and gannet flocks, glowing portable DO, no sea/air/land selector, no fern, supporting customer journey and reward story.

## Verified locally

- Desktop at the browser default 1710 × 891 and mobile at 375 × 700: readable hero, real generated motion, all three product links, shaped DO launcher, no horizontal overflow.
- DO launcher opens a keyboard-dismissible task workspace and remains fixed through the page.
- Blender DO model loads in WebGL and assembles with scroll. Replaced the discarded fern completely.
- Corrected dark headings in the plum chapter and moved the launcher outside the clipped hero.
- Video has an explicit pause/play control, a poster fallback, visibility/intersection pause and reduced-motion handling. The film viewer has native controls and close.
- Six source-based task choices. Consent is required before preparation; source review and output review are distinct. Edits clear recorded review. Optional browser saving is separate.
- Public copy has no named client examples. Pursuit and Studio lead to existing authenticated workspaces; public product enquiries offer separate or combined engagements.

## Validation

Production build and TypeScript passed. 39 focused tests passed. Repository lint passed. Changed-file lint includes an explicit Three.js scene-mutation exemption for the renderer-owned environment.

## Release checks

Production provider and final public URL checks are recorded with the release handoff. Messaging use is select/paste and reviewed copy-back; no native WhatsApp/SMS connector or background monitoring is claimed. Browser extension is a direct-install package, not a store listing.

## Visual assessment

No unresolved P0/P1/P2 visual issues in the reviewed homepage. Browser screenshots are in the task output release-checks folder. The chosen source composition is intentionally adapted for narrow screens so the headline, task entry and film controls fit.
