name: builderdoo validation

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'app/api/do/builder/**'
      - 'app/do/builder/**'
      - 'app/do/office/**'
      - 'apps/do/shared/builder*'
      - 'apps/do/builder-runner/**'
      - 'docs/factory/PRIMITIVES.md'
      - '.github/workflows/builderdoo-validation.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build shared canvas
        run: pnpm --filter @assembl/canvas build
      - name: Typecheck
        run: pnpm typecheck
      - name: Builderdoo unit test
        run: pnpm test apps/do/shared/builder.test.ts
      - name: Production build
        run: pnpm build
