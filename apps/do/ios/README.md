# DO native (iOS)

Compiling-ready stubs for assembl DO distribution layers. Linux CI cannot App Store–build these.

| Target | Path |
|--------|------|
| Keyboard Extension | `DoKeyboard/` |
| Needs you WidgetKit | `DoNeedsYouWidget/` |

See each folder’s README for Xcode steps, Full Access trust warning, and API base config.
