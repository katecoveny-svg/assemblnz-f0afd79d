# DO native (Android)

Compiling-ready stubs for assembl DO distribution layers. Linux CI cannot Play–build these.

| Target | Path |
|--------|------|
| IME keyboard | `DoIme/` |
| Needs you App Widget | `DoNeedsYouWidget/` |

See each folder’s README for Android Studio steps, trust warning, and API base config (`10.0.2.2:3000` for emulator → host).
