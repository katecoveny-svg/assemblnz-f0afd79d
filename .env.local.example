# ────────────────────────────────────────────────────────────────────────────
# .env.local — local dev only. NEVER commit .env.local.
# Vercel project env holds the production values; copy them down for local
# dev (Vercel CLI: `vercel env pull .env.local`).
# ────────────────────────────────────────────────────────────────────────────

# Canonical assembl-prod Supabase project (project ref: ssaxxdkxzrvkdjsanhei).
# Get these from Supabase Studio → Project Settings → API.
# IMPORTANT: do NOT use the legacy Lovable values (project ref:
# wurwcrgxjjwqdaxqceey) that live in the older `.env` file at the repo root —
# those point at the old Lovable Supabase and are now stale for auth.
NEXT_PUBLIC_SUPABASE_URL="https://ssaxxdkxzrvkdjsanhei.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Optional — founder gate cookie passphrase for /dashboard/vessel-studio.
# Out of scope for the Supabase auth wiring.
# FOUNDER_GATE_SECRET=""
